services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: adote-pets-api
    env_file:
      - .env
    ports:
      - "${PORT:-3000}:3000"  
    volumes:
      - ./:/app               
      - /app/node_modules   
    restart: always
    networks:
      - adote-pets-network

  prisma-studio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: adote-pets-prisma-studio
    env_file:
      - .env
    ports:
      - "5555:5555"
    volumes:
      - ./:/app
      - /app/node_modules
    command: npx prisma studio --port 5555 --hostname 0.0.0.0
    depends_on:
      - app
    restart: always
    networks:
      - adote-pets-network

networks:
  adote-pets-network:
    driver: bridge           